// Route "/profile" — renders the Profile screen UI from the screens folder.
import ProfileScreen from '../../screens/profileScreen';

export default function Profile() {
    return <ProfileScreen />;
}
